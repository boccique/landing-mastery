const person = {
    firstName: "John",
    lastName: "Doe",
    age: 21,
    location: ['Planet', 'Earth'],
    applied: false,
};

console.log(`${person.firstName} is not applied to the job? ${person.applied}`);